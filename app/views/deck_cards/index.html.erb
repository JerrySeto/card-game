<h1>Listing deck_cards</h1>

<table>
  <thead>
    <tr>
      <th>Deck</th>
      <th>Card</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @deck_cards.each do |deck_card| %>
      <tr>
        <td><%= deck_card.deck_id %></td>
        <td><%= deck_card.card_id %></td>
        <td><%= link_to 'Destroy', deck_card, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
